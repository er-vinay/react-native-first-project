import { View, Text } from 'react-native';
const Header = () => {
    return (
        <View>
            <Text style={{fontSize: 20, textAlign: 'center', marginTop:20}}>Header</Text>
        </View>
    );
};
export default Header;