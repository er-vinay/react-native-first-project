import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    text: {
        color: '#fff',
        marginVertical: 10,
        backgroundColor: 'red',
        fontSize: 20,
        padding: 10,
        textAlign: 'center'
    }
});

export default styles;